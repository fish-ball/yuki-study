# 每日小结

执行 `.cursor/skills/daily-summary/SKILL.md`。

默认日期为今天。必须同时统计：

1. 管理台做题时长（合并学段 + 纯作答）
2. Cursor 对话 session（条数、主题、时长；学科 / 系统）
3. 两者去重后的合计投入，以及按小时四列统计
4. 学习内容（知识点、等级、正确率、错因）
5. 当天系统 / Harness 改进（对上对话，不写密钥）

写入 `plans/summaries/YYYY-MM-DD.md`，并在对话里用同一结构发给学生。
